# Book Extracts Frontend (React)

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start frontend:

```bash
npm start
```

Frontend runs on:

- `http://localhost:3000`

## Backend dependency

The frontend expects the backend API at:

- `http://localhost:7071/api/books`

Start backend first to avoid error state on initial load.
